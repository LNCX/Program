#include<bits/stdc++.h>
using namespace std;
const int N=55;
int loc[N],in[N];
bitset<N>vis;
bool dfs(int u)
{
    
}
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        int n;
        cin>>n;
        for(int i=1;i<=n;i++)
            scanf("%d",&loc[i]);
        for(int i=1;i<=n;i++)
            scanf("%d",&in[i]);
    }
}