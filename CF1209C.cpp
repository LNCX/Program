#include<bits/stdc++.h>
using namespace std;
const int N=2e5+5;
char s[N];
int main()
{
    int t;
    scanf("^%d",&t);
    while(t--)
    {
        int n;
        scanf("%d%s",&n,&s+1);
        vector<int>a,b;
        for(int i=1;i<=n;i++)
        {
            if(s[i])
        }
    }
    return 0;
}